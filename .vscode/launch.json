{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "fnm env | Out-String | Invoke-Expression && ./node_modules/.bin/astro dev",
      "name": "Development server",
      "request": "launch",
      "type": "node-terminal"
    }
  ]
}
